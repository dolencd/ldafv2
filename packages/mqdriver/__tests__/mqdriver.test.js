'use strict';

const mqdriver = require('../dist/mqDriver');

describe('@ldafv2/mqdriver', () => {
    test("test", () => {
        console.log("MQDriver needs tests")
        expect(1).toBe(1);
    })
    

});
